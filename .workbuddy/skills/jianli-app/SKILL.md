---
name: jianli-app
description: 本技能用于开发、维护、扩展「渐离App」(jianli-app) —— 一个 Electron + Vue3 + TypeScript + Vite 桌面效率应用。当任务涉及该项目的任意模块（习惯打卡、番茄钟、待办、剪贴板、笔记、电子书、股票、截图、小窗、提醒引擎、命令面板、调色板、数据层等）、需要理解架构/进程边界/IPC 通道/小窗四件套/复用模式，或要新增功能、排查已知雷区时，使用本技能。
agent_created: true
---

# 渐离App 项目开发技能（jianli-app）

## 这是什么
封装「渐离App」整个桌面应用的架构、约定、IPC 契约、复用模式与逐模块知识，让 AGENTS 在本项目里能按既定模式开发、维护、扩展功能，并避开已知雷区。它不是运行时功能，而是「开发该应用的知识库」——把散落在代码与记忆里的工程约定固化下来，供后续会话按需加载。

## 何时使用
- 任务涉及本项目任意模块（习惯打卡 / 番茄钟 / 待办 / 剪贴板 / 笔记 / 电子书 / 股票 / 截图 / 小窗 / 提醒 / 命令面板 / 调色板 / 数据层 …）。
- 需要理解：进程边界、改主进程要不要重启、IPC 通道怎么对接、小窗怎么加、某个注册表怎么扩。
- 要新增功能、重构功能、排查问题、排查「发了 IPC 没反应」、避免 newSql / 穿透 / 破表等历史坑。
- **数据层操作 / 双 SQL 层合并**：建表、读写业务数据、多库选择、旧层调用迁移，见「参考文档导航」中的 `sql-db-ops.md` 与 `sql-merge-plan.md`。
- **把功能移植到 Flutter 移动端（Android/iOS）、跨端复用 db.sqlite、类 LocalSend 局域网同步**：见下方「Flutter 移动端移植计划」与 `references/flutter-port.md`。

## 全局红线（先读，违反必踩雷）
1. 渲染端**禁止 `import electron/*`（含类型）**；一切系统 / 磁盘 / 库操作走 IPC。
2. 改主进程（`electron/**`）**必须重启 Electron**；改渲染端（`src/**`）热重载即可。
3. 业务数据走 newSql 的 `query`/`upsert`/`delete`；❌ **严禁裸 `new-sql:execute`**（确需用 SQL 时先读 `references/db-pitfalls.md`，execute 有自动建表劫持结构、PRAGMA 拿不到结果、SELECT 结果在 data.rows 三个必避的坑）。
4. SQLite 补主键 = `ADD COLUMN key TEXT` + `CREATE UNIQUE INDEX`（不能 `ALTER` 加 PK）。
5. 常驻小窗必须 `mouseEvents:true`，否则鼠标穿透点不动 / 拖不动。
6. 小窗路由 path 名**必须**与主进程 `createOtherWindow` 的 `arg` 一致。
7. 新需求开发/功能重构采用**原子化、组件化、功能化**拆解构建：单文件职责单一、体量可控，禁止把一堆功能堆成一个超大文件（与项目 `AGENTS.md`「功能注意分割，防止代码文件过大」一致）；每个功能 / 组件需带注释。
8. 新需求开发落地清单（接入菜单与小窗）：
   - **必备**：① `src/router/index.ts` 的 `RouteNames` 加 key + `layoutRouters` 注册路由；② **`src/constants/menu.ts` 的 `menuGroupDefs` 对应分组 `names` 加名**（菜单分组唯一数据源，侧边栏 `src/layout/index.vue` 与路由配置页 `src/views/routeSetting/index.vue` 自动同步，❌ **严禁分别去这两个文件里加名单**——2026-09-10 已因两份名单漂移导致 6 个功能在配置页没有开关）；③ 可选：`src/utils` 的 `iconMap` 加图标。
   - **可选**：若需常驻浮动交互，再按小窗四件套加一个小窗（见 `references/mini-window.md` / `references/modules/small-window.md`），且必须 `mouseEvents:true` 并遵循路径一致性红线（第 6 条）。
9. 每次修改都必须同步更新对应模块的文档 `references/modules/<模块>.md`
10. **导出统一规范（见 `references/export.md`，新增导出功能前必读）**：所有「导出 / 保存文件到磁盘」走统一入口 `src/utils/exportToFile.ts`（`exportTextToCache` / `exportBufferToCache`）；**不弹系统保存框、默认直写缓存目录 `fileCachePath`、成功用 `src/utils/fileNotify.ts` 的 `fileNotify` 提示（蓝色可点击路径）**；安全敏感导出（2FA 密钥库 / 文件保险库解密）保留用户选位置，仅把成功提示换成 `fileNotify`。

## 参考文档导航
- 架构总览：`references/architecture.md`
- 数据层约定：`references/data-layer.md`
- SQL 数据库操作（统一数据层 newSql）：`references/sql-db-ops.md`
- 双 SQL 层合并设计：`references/sql-merge-plan.md`
- 数据库踩坑指南（写库前必读）：`references/db-pitfalls.md`
- IPC 通道契约：`references/ipc-channels.md`
- 导出统一规范（落盘/命名/反馈/红线）：`references/export.md`
- 小窗机制与四件套：`references/mini-window.md`
- 通用复用模式：`references/patterns.md`
- 主题与视觉约定（token 清单 / 严禁硬编码 / 禁用未全覆盖的 --el-* / 派生色用 color-mix）：`references/theme.md`
- 已知差异与风险：`references/risks.md`
- Flutter 移动端移植计划（跨端 / 双端同步）：`references/flutter-port.md`
- 文件互传双端方案与决策记录（**已实施完成**，2026-09-06；同日追加「桌面端 `transfer:cancel` 取消批次 / `scanPeers()` 热点定向广播 / 第一批 sha256 双端校验·历史 `error` 列·进度节流 + `.part` 清理 + `incoming` 回收·`peer_name` 回填·桌面端历史打开·定位·批次总进度·速率·ETA·移动端历史打开·分享」；**二批（M12/T12）再补 #9 重名覆盖 / #10 文件夹·拖拽 / #11 最近设备 / #13 断点续传 / #14 会话加密(AES-256-CTR,默认关) / #15 接收询问 / #16 磁盘预估 / #17 并发守卫 / #19 移动后台保活 / #20 历史分页+自动清理**，全部向后兼容）：`references/file-transfer-plan.md`；模块文档：`references/modules/file-transfer.md`
- 逐模块文档（`references/modules/`，处理具体模块前先读对应文件）：
  - **效率 / 提醒类**：`habit` `reminder` `todo` `pomodoro` `countdown` `command-palette` `theme-conversation` `window-mode` `shortcut` `home-mode` `route-setting` `settings` `quick-note` `sticker` `app-lock` `two-factor` `file-vault`
  - **内容 / 数据类**：`clipboard` `notebook` `categorizable-notes` `ebook-reader` `accounting` `stock` `flow` `function` `color-palette` `resume`
  - **系统 / 工具类**：`system-info` `weather` `crawler` `spider` `high-perf-sql` `file-rela` `resource-manage` `screenshot` `browser` `downloader` `about` `safety-protection` `app-cache` `backup` `tts` `small-window` `home` `data-acquisition` `dev-toolbox` `qr-code` `sync` `file-transfer` `ferry` **`note-slip`（小纸条，P1-6 双端同名）**

## 使用方式
1. 接到本项目任务，先判断属于「架构 / 数据 / IPC / 小窗 / 复用模式」哪一类，读对应核心参考。
2. 锁定到具体模块，读 `references/modules/<模块>.md` 拿到入口文件、store、路由、用到的 IPC、特有坑。
3. 需要新增能力时，优先复用既有模式（命令面板 REGISTRY、链式动作 registry、小窗四件套、提醒引擎 `syncReminders`），不要另起炉灶。
4. 所有文档用中文；发现与代码不符，请直接更新对应文档，保持 skill 与代码同步。

## 维护说明
- 本 skill 是「项目知识基线」，随代码演进而更新。每次大改动后同步 `risks.md` 与对应模块文档。
- 2026-09-06：**双端「文件互传」已实施完成**（批量收发、双端对称），并完成两批后续。桌面端 `electron/main/module/transfer/transferModule.ts`：`initTransfer()` 注册 /file/* 三端点 + 发送客户端（流式 + 进度节流 + 算 sha256 + 可 `transfer:cancel` 中止）+ 历史读写（含 `error` 列）+ IPC（status/scan/pick-files[#10 支持选目录]/send/cancel/history[#20 分页]/open-received/open-file/open-folder/set-auto-accept/**set-rename(#9)/set-enc(#14)/recent-peers(#11)/forget-peer(#11)/answer-offer(#15)**）+ 4 类事件（progress/received/batch-done/**incoming-ask(#15)**）；`syncModule.ts` 数据面可插拔路由 `registerDataRoute` 且 `scanPeers()` 支持热点定向广播 + 网关单播；`sweepStale()` 定时清理残留 `.part` 与超时 `incoming`。二批增强（T12，向后兼容）：#9 重名覆盖 / #10 文件夹·拖拽 / #11 最近设备 / #13 断点续传（data `?from=N` + hash 播种，加密批次整文件重发）/ #14 会话加密（AES-256-CTR，offer `enc` 协商，默认关）/ #15 接收询问（pendingAsk + `incoming-ask` 事件，60s 超时拒）/ #16 磁盘预估（`fs.statfsSync`，不足 `507`）/ #17 并发守卫（activeReceiveTid/activeSendTid，冲突 `429`）/ #20 历史分页 + `trimHistory(1000)`。渲染端 `src/views/fileTransfer/`（薄壳 + DeviceList/TransferPanel[含批次总进度·速率·ETA + 取消 + 最近设备·重名·加密·询问开关]/TransferLog[可打开·定位·加载更多 + 失败原因] + api + `useFileTransfer` store[批次聚合]），菜单四件套接入（路由 `/fileTransfer`、iconMap `ArrowLeftRight` 已加进 `LucideIcon` nameMap）、`layout/index.vue` 监听 `file-transfer:received` → `fileNotify`；移动端同协议同历史表（含 `error` 列、sha256、offer `me` 回传、历史打开/分享、#9/#11/#13/#14/#15/#16/#17/#19/#20 同语义）。决策记录：`references/file-transfer-plan.md`；模块文档：`references/modules/file-transfer.md`。改主进程**必须重启 Electron**；移动端改 `tables/file_transfer.dart` 后**须 `dart run build_runner build -d`**。
- 2026-09-05：**局域网同步白名单扩容（移动端主题对话对齐）**——`syncModule.ts` 与 `src/store/useSync.ts` 加入主题对话三表 `conversation_theme` / `conversation` / `conversation_tag`（INTEGER 自增 id 主键）；新增 `tablePk()` 按表适配主键（conversation* → `id`，其余 → `key`），upsert 走 `ON CONFLICT(pk) DO UPDATE`。详见 `references/modules/sync.md` 白名单小节；**改完需重启 Electron**。
- 新增模块时：在 `references/modules/` 加一份文档，并在上方「逐模块文档」导航里补一行。
- 2026-09-19：**P1-6「小纸条」双端落地（页面名双端统一【小纸条】）**。定位 = PC ⇄ 手机 文字/链接速传（单条 ≤8000 字），**大文件仍走文件互传**。① **协议**：复用 47124 数据面新增 `GET /slip/ping` + `POST /slip/push`（`registerDataRoute`），发现复用 47123；body `{id,from:{name,platform},kind,content,ts}`，**`id` 即幂等键**（重复推送只更新不重复弹通知）；`platform` PC = `win32-electron`、手机 = android/ios，是筛选对端的唯一依据（PC 剔除 electron、手机只列 PC）。② **表 `note_slip` 双端同构**（TEXT 主键 key；direction/kind/title/content/peer_name/peer_ip/read/created_at），**不入同步白名单**（三处白名单都不动）；PC 走 `ensureTableExists(..., 'key', {primaryKeyType:'TEXT'})`，移动端 schemaVersion 5→6 + `dart run build_runner build -d`。③ **PC**：`electron/main/module/noteSlip.ts`（建表 + 两端点 + 9 条 IPC `slip:scan/targets/last-peer/send/send-clipboard/list/read/delete/clear` + 事件 `slip:received` + 导出 `sendClipboardSlip`），注册进 `runDeferredInits` 且**必须排在 sync 之后**；渲染端 `src/views/noteSlip/`（types/api/SlipCompose/SlipList/index）+ `src/store/useNoteSlip.ts`；菜单四件套（路由 `/noteSlip`、`menu.ts`「系统与资源」组、`iconMap.noteSlip='StickyNotePlus'`）；`LucideIcon` 新增 `ClipboardCopy`/`ArrowDownLeft`/`ArrowUpRight`（**import 与 nameMap 两处都要加**）；`layout/index.vue` 监听 `slip:received` → `ElNotification` 点击直达页面。④ **PC 三入口**：页面输入 + 全局快捷键（registerShortcut type `send_clipboard_slip`，默认 `Ctrl+Alt+S`）+ 托盘「发小纸条（剪贴板）」，回执走 `notifyTrayBalloon`。⚠️ Electron 读不到别的进程选区，只能读剪贴板，故「选中文字即发送」不存在。⑤ **移动端**：`lib/features/note_slip/`（models/repositories/services/providers/bootstrap/intake/components）+ 路由 `/slip` + 工具分组页入口 + 通知渠道 `slip`（High+Public，键名锁定）+ 点击跳详情抽屉；**接收时机双开关**（用户拍板）basic_info `slip_always_on` = 1 冷启动常驻 / 0 仅开页面时可收。⚠️ 两条移动端铁律：`registerRouteHandler` 必须幂等注册（否则一次请求处理两遍、弹两条通知）；数据面全局共享（开过同步/互传页照样在跑），不是 bug。模块文档 `references/modules/note-slip.md`；`vue-tsc --noEmit` 与 `dart analyze` 均零错误。**改主进程必须重启 Electron**。
- 2026-09-06：**（⚠️ 已于 2026-09-11 重新启用，见本列表最新一条「重新启用 PDF 工具箱右键菜单」）资源管理器 PDF 右键菜单曾临时禁用（仅注释，未删除）**。改动文件 `electron/main/module/shellMenu.ts`：① `SUB_COMMANDS` 中 5 条 `.pdf` 子命令（`JianliApp.PdfCompress/Split/Merge/ExtractAttach/ToImage`，即「PDF 压缩/拆分/合并/提取附件/转图片」）整体注释掉；② 新增模块级常量 `DISABLED_PDF_IDS` 与 `cleanupLegacy()` 中的清理循环，确保已安装机器注册表里的残留 PDF 右键键被删除（否则菜单不会消失）。**原因**：`registerShellMenu()` 在启动期对每条命令各触发多次 `reg` 写入，严重拖慢启动；经确认仅临时关闭右键菜单。**范围**：仅右键菜单；App 内「PDF 工具箱」页（`pdf.ts` 的 `initPdf()`，日志 `[pdf] PDF 工具箱 IPC 已注册`）保持可用，`index.ts` 的 `registerShellMenu()` / `initShellMenu()` / `initPdf()` 调用均保留。**恢复方法**：取消 `shellMenu.ts` 中 5 条 PDF 子命令注释、删掉 `DISABLED_PDF_IDS` 常量及其在 `cleanupLegacy()` 里的清理块，重新打包即可。**改主进程必须重启 Electron**；构建产物（`dist-electron/`、`release/`）会在下次打包时重生成，勿手改。
- 2026-09-06：**隔空互传（QRFerry 本地集成）已落地** —— 新增 `electron/main/module/ferry.ts`（`initFerry()` 注册 `ferry:open` IPC + 起 127.0.0.1 随机端口本地静态服务 + 独立 `BrowserWindow`（`partition:"ferry"` 隔离会话）加载 qyferry + 摄像头权限自动放行 + 接收文件落盘「文档/隔空互传/」）。资源 `electron/resources/qyferry` 经 `electron-builder.json5` 的 `extraResources` 打包进 `resources/qyferry`；渲染端 `src/views/ferry/index.vue` 为入口页（按钮走 `ferry:open` IPC），菜单四件套（路由 `/ferry`、iconMap `ScanQrCode`、侧边栏与 routeSetting「系统与资源」组）。模块文档 `references/modules/ferry.md`。**改 main/ferry.ts 必须重启 Electron**；qyferry 静态站本身为纯前端、自带扫码解码，无需自写扫码逻辑，仅开放摄像头权限。详见 `references/modules/ferry.md`。
- 2026-09-11：**资源管理器右键菜单注册改为「变更驱动 + 幂等覆盖」，解决启动变慢**。原 `registerShellMenu()` 每次冷启动都 `cleanupLegacy()` 全量 `reg delete` + 逐命令 `reg` 重建（含 `resolveProgId` 每条扩展名 3 次 `reg query`）+ 末尾 powershell `SHChangeNotify`，约上百次同步子进程调用阻塞主线程，是启动缓慢主因之一（此前 PDF 5 条命令即因同样原因被临时禁用）。改动（`electron/main/module/shellMenu.ts` + `electron/main/index.ts`）：① `cleanupLegacy(enabled?)` 只删「被用户禁用命令」与「DISABLED_PDF_IDS 残留」，启用项改 `reg add /f` 幂等覆盖（移除逐叶 `regDeleteTree`）；② 新增 `isShellMenuUpToDate()`（比对 `basic_info.shellMenuRegistered` marker + `shellMenuSchema`），`index.ts` 启动处仅在「未注册 / 版本变更」时调 `registerShellMenu()`，日常冷启动完全跳过；③ 设置页开关/默认打开/重新注册 IPC 仍显式调 `registerShellMenu()` 不受跳过影响，注册成功写 marker。附带修复：此前「在设置页禁用某命令」不会从右键菜单移除（因 cleanupLegacy 未删启用集外的项），现 cleanupLegacy 按 enabled 集清理，禁用即移除。详见 `references/modules/file-vault.md` 的「资源管理器右键菜单」章节。**改主进程必须重启 Electron**。
- 2026-09-11：**启动提速（P0 埋点 + P1 延迟非关键初始化）**。`electron/main/index.ts` 的 `createWindow()` 原把所有模块 `initX` 串行跑在主线程（窗口 show 后仍冻结 ~5s）。改动：① 新增 `timeInit(name, fn)` 包裹每个 init 打印 `[init] <name>: <ms>`，定位冷启动瓶颈（此前日志只有 TTS/ebook 有计时，ebook→pdf 间 4.5s 为盲区）；② 新增 `yieldToEventLoop()`（setTimeout 0 让出一帧）；③ 新增 `runDeferredInits()`，把非关键模块（ebook / ebookTransfer / screenshot / stock / sinaFinance / browser* / downloader / resume / qrCode / sync / transfer / ferry / shellMenu / shellMenuIpc / pdf）移出首屏关键路径，`setTimeout(…,0)` 延迟到首屏之后分批执行，且每个之间 `yieldToEventLoop`，使主窗口启动即可交互、消除主线程冻结；④ 关键路径（DB / 提醒 / tray / 应用锁 / 2FA / 密码保险库 / 文件保险箱 / 协议 / 新窗口 / 剪贴板 / 快捷键等）保留在 `initMainWindow()` 之后立即执行。注意：`runDeferredInits` 中的模块 IPC 在延迟期内才注册，若某页面在首屏瞬间就调用这些 IPC 可能早于注册（风险低，多在用户导航后）；如遇某功能首屏即需其 IPC，将其移回关键路径即可。**改主进程必须重启 Electron**；验证用启动日志的 `[init]` 计时。
- 2026-09-11：**启动提速第二轮（P1 + 字体懒加载）**。基于 `[init]` 计时日志定位：`initSys`（`getFonts2` 枚举系统字体）耗时 **3099ms** 且位于关键路径、阻塞首屏；`file-vault:status` 报 `No handler registered`（P1 让主线程变自由后，渲染端启动即调用该 IPC，但 `initFileVault` 原在关键路径最末尾、`sys` 之后才注册，handler 未就绪）。改动（`electron/main/index.ts` + `electron/main/module/sys.ts`）：① `initSys` 移出关键路径、加入 `runDeferredInits()`（仅注册 IPC，启动即用不到）；② 把 `initTwoFactor / initPasswordVault / initFileVault` 紧跟 DB 之后立即注册（与 `appLock / safetyProtection` 一起提前到关键路径最前），确保渲染端挂载前安全/锁类 handler 已就绪，消除 `file-vault:status` 报错；③ `sys.ts` 的 `get-fonts` 改为**懒加载**——首次调用时才 `getFonts2()` 并缓存，init 本身不再阻塞于字体枚举（连延迟批次里的 3s 也消除，用户不打开字体选择器则永不触发）。效果：关键路径从 ~3.3s 降到 ~150ms、首屏立即可交互；`file-vault:status` 报错消失。`shellMenu` 的 3750ms 为「改动后首次启动一次性」（写 marker 后下次跳过=0ms），且已延迟不阻塞首屏。**改主进程必须重启 Electron**；验证：重启后看 `[init]` 计时（sys 应不在关键路径、file-vault 报错应消失）。
- 2026-09-11：**启动提速第三轮（方案 A：右键菜单注册延迟到空闲期静默执行）**。基于前两轮：右键菜单注册已靠 `isShellMenuUpToDate()` 幂等跳过（第 2 次及以后启动 = 0ms），但「首次注册 / schema 升级」那一次 ~3.7s 的 `execFileSync` 注册表写入仍排在 `runDeferredInits` 的延迟批次里（首屏后立刻跑，仍算启动窗口内）。改动（`electron/main/index.ts`）：① 把 `['shellMenu', () => { if (!isShellMenuUpToDate()) registerShellMenu(); }]` 从 `runDeferredInits` 步骤表移除（保留 `['shellMenuIpc', initShellMenu]`，那是设置页 IPC 注册、<1ms 需保留）；② 在 `setTimeout(runDeferredInits, 0)` 之后新增独立空闲定时器 `setTimeout(…, 3000)`，3s 后才 `if (!isShellMenuUpToDate()) registerShellMenu()`，并打印 `[shellMenu] 空闲期静默注册右键菜单…/完成`，异常仅日志不中断。效果：右键菜单注册彻底移出启动时长——第 2 次起 0ms；仅首次/升级那次 3.7s 被推到「系统打开 3s 后」静默执行（仍用同步 `execFileSync`，故该时刻会短暂阻塞主线程一次，仅发生一次，可接受）。**改主进程必须重启 Electron**。
- 2026-09-11：**启动提速第四轮（方案 B：右键菜单注册改 Worker 线程，并移除 DB 跳过逻辑）**。用户反馈方案 A 后仍鼠标卡顿（根因：`regAddDefault`/`regSet`/`regDeleteTree`/`notifyShellRefresh` 内部仍用同步 `execFileSync`，方案 A 只推迟了「何时跑」、没改「怎么跑」，3.7s 注册表写入仍一次性阻塞主线程）。改动（`electron/main/module/shellMenu.ts` + 新增 `public/worker/shellMenu.cjs` + `electron/main/variables.ts` + `electron/main/index.ts`）：① 引入 `RegOp` 规划类型 `{ cmd: 'reg'|'powershell'; args: string[] }`；`regAddDefault`/`regSet`/`regDeleteTree`/`registerCommandLeaf`/`registerDefaultOpenProgIds`/`setDefaultOpen`/`notifyShellRefresh`/`cleanupLegacy` 全部改为「把操作推入传入的 `ops: RegOp[]`」、不再立即执行；② 新增 `runShellMenuWorker(ops)`：`new Worker(shellMenuWorkerPath)` 把 `RegOp[]` 发给 `public/worker/shellMenu.cjs`，Worker 用异步 `execFile` 逐条执行、收尾 `parentPort.postMessage('done')`，主线程全程零阻塞；Worker 起不来时回退主线程 `execFileSync` 兜底；`variables.ts` 按 dev/packaged 解析 Worker 路径（`./public/worker/shellMenu.cjs` / `./dist/worker/shellMenu.cjs`）。③ 用户明确「每次启动都跑也行、完全不需要数据库」→ **删除 `isShellMenuUpToDate()` 与 basic_info 的 `shellMenuRegistered`/`shellMenuSchema` marker 及写入逻辑**（此 marker 此前还因 node-sqlite3 回调误用导致恒为 false，等于每次都重写注册表）；`registerShellMenu()` 现每次启动无条件经 `setTimeout(…,3000)` 触发、`await runShellMenuWorker(ops)`。保留 `shellMenuEnabled`/`shellMenuDefaultOpen` 配置读取（`getEnabledIds`/`readDefaultOpen`，快速异步）。效果：注册表写入全在 Worker 线程异步跑，主线程事件循环不再冻结——鼠标/窗口卡顿彻底消除；每次启动都重写注册表，但因不阻塞主线程、且启用项走幂等 `reg add /f` 覆盖，用户体验无损。**改主进程必须重启 Electron**；类型校验 `vue-tsc --noEmit` 交用户本地跑（超 30s 不代跑）。
- 2026-09-11：**右键菜单注册日志「`op failed` 刷屏」排查与修复**。现象：空闲期注册日志大量 `[shellMenuWorker] op failed: reg delete ... 系统找不到指定的注册表项/值`，并出现 `(默认值未设置)` 混入注册表路径。根因两类：① **真 bug——`regGetValue`/`regGetDefault` 把「默认值未设置」的本地化占位符当成真实值返回**：`reg query ... /ve` 在默认值空时输出中文 `(默认值未设置)` / 英文 `(value not set)`，`/REG_SZ\s+(.+)/` 误捕获为值 → `resolveProgId` 误当 ProgID → `shellParentsFor` 拼出垃圾父键 `HKCU\Software\Classes\(默认值未设置)\shell`，对每条命令都去删/写该垃圾路径（写入是静默的，注册表留脏键）。修：`regGetValue` 命中占位符即返回 `null`（也覆盖 `(未设置)`）。② **双 `JianliApp.` 前缀 + `HKCU_ROOT` 误删**：`s.id` 已含 `JianliApp.`，但 `registerCommandLeaf`/`cleanupLegacy` 又拼一次 → `JianliApp.JianliApp.X`；且 line 290/301 用 `HKCU_ROOT`（`*\shell\JianliApp`）对非 `*` 命令错误删到 `*` 键下。修：`registerCommandLeaf` 的 base 改 `${parent}\\${s.id}`（单前缀）；删除 `HKCU_ROOT` 常量及其两处用法，统一改由 `shellParentsFor` 决定父键；`cleanupLegacy` 抽 `cleanLeaf`，对每个 id 同时删「正确单前缀」与「历史双前缀孤儿键」两种残留（reg delete 删不存在的键本就静默失败、无副作用）。③ **Worker 抑制良性噪声**：`reg delete` 删不存在的键是 cleanup 的正常结果，不应报 `op failed`；改 Worker 对 `reg delete` 失败静默忽略，只记录 `reg add`/`powershell` 的真实失败。效果：日志不再有 `(默认值未设置)` 垃圾路径，残留的「key not found」删除是该扩展名从未装过对应命令的正常清理，无害；注册表不再产生脏键。改动文件 `electron/main/module/shellMenu.ts` + `public/worker/shellMenu.cjs`。**改主进程须重启 Electron**；`vue-tsc` 交用户本地跑（超 30s 约定）。
- 2026-09-11：**右键菜单出现「同名重复项」修复（第五轮改动的回归）**。现象：资源管理器右键菜单里「批量重命名 / 加密到保险箱 / 安全删除」等每条命令各出现两次。根因：早期版本把动词键写成**双前缀** `JianliApp.JianliApp.x`；第五轮改为单前缀 `JianliApp.x` 后，`cleanupLegacy` 只在「命令被禁用」时删旧键，**已启用命令的旧双前缀键未被清**，新旧两键并存 → Explorer 显示两份。修（`electron/main/module/shellMenu.ts`）：`cleanupLegacy` 重写为——**无论是否启用，始终清掉「历史双前缀」孤儿键**（`deleteVerb(id, exts, legacyOnly=true)`）；仅对「被禁用」命令才删正确单前缀键 + HKLM CommandStore。重启一次 Electron 即自动清除旧双前缀键、重复项消失。**改主进程须重启 Electron**。
- 2026-09-11：**重新启用「PDF 工具箱」右键菜单（与其他右键命令一起注册）**。背景：2026-09-06 曾因「5 条 `.pdf` 命令在启动期各触发多次注册表写入、严重拖慢启动」而把它们在 `SUB_COMMANDS` 里整体注释（并加 `DISABLED_PDF_IDS` + `cleanupLegacy` 清理残留）。现第四轮已把全部注册表写入搬到 Worker 线程、不阻塞主线程，拖慢启动的根因已消除，故恢复。改动（`electron/main/module/shellMenu.ts`）：① 取消 `SUB_COMMANDS` 中 5 条 PDF 命令注释（`JianliApp.PdfCompress` / `PdfSplit` / `PdfMerge` / `PdfExtractAttach` / `PdfToImage`，`exts: ['.pdf']`）——它们随其他命令一起被 `registerShellMenu()` 注册（动词键 `HKCU\Software\Classes\.pdf\shell\JianliApp.PdfXxx`）；`parseCliFiles` 的 `flagToAction`（`--pdf-*`）、渲染端 `App.vue` 的 `pdf-*` 分发与 `usePdfTools` 早已就绪，**无需改动别处**；② 删除 `DISABLED_PDF_IDS` 常量及其在 `cleanupLegacy` 中的清理循环（PDF 已不再是「已移除命令」）；③ **`SHELL_MENU_SCHEMA` 2 → 3**：PDF id 此前不在 `SUB_COMMANDS` 中，旧存档（schema≥2）的 `shellMenuEnabled` 可能不含它们而致默认不注册，+1 触发一次启用集合迁移让 5 条 PDF 默认启用（迁移只重置「非传统命令」，3 个保险箱命令仍尊重用户旧开关）。App 内「PDF 工具箱」页保持可用。**改主进程必须重启 Electron**；`vue-tsc` 交用户本地跑（超 30s 约定）。
